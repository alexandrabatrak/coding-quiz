export function scores() {
  let finalScore = document.getElementById('final-score');
  let initials = document.getElementById('initials');

  let score = 0;

  let highScores = [{ score: score, initials: initials }];
}
